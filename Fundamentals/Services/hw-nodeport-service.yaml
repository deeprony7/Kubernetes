apiVersion: v1
kind: Service
metadata:
  name: hw-service
spec:
  selector:
    app: hellopod
  ports:
  - port: 31001
    nodePort: 31001
    targetPort: nodejs-port
    protocol: TCP
  type: NodePort
